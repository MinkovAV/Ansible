---
- hosts: all
  gather_facts: no
  become: yes
  roles:
    - { role: prepare_os, tags: docker }
    
    #- { role: docker, tags: docker }
    
    #- { role: minio, tags: docker }

    #- { role: test, tags: docker }

    #- { role: jenkins, tags: docker }

...